<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex-box">
    <h1 class="font-bold text-4xl">Toetsen</h1>
    <%= link_to "New test", new_subject_test_path, class: "new-button" %>
  </div>

  <div id="tests" class="min-w-full">
    <%= render @tests %>
  </div>
</div>
